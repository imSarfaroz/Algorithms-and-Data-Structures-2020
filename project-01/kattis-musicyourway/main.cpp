#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main()
{
    string atributes;
    getline(cin, atributes);

    //taking songs
    int numOfSongs;
    cin >> numOfSongs;
    for (int i = 0; i < numOfSongs; i++)
    {
        vector<string> song;
    }
}